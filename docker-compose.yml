services:
  polyfut:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./sample_data:/app/sample_data:ro
      - polyfut-data:/data
    environment:
      - OMP_NUM_THREADS=2
      - MKL_NUM_THREADS=2
    command: python server.py

volumes:
  polyfut-data:
