# Legacy polyfut_cv (superseded)

The **Level 1** possession pipeline lives in [`../polyfut_video/`](../polyfut_video/).

`polyfut_cv` is retained for reference only and is **no longer** used by `server.py`.

Install and run:

```bash
cd PolyFut-Clean
pip install -r requirements.txt
python server.py
```
